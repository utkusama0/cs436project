apiVersion: v1
kind: Service
metadata:
  name: student-service
  namespace: student-management
spec:
  selector:
    app: student-service
  ports:
  - port: 8080
    targetPort: 8080
  type: ClusterIP