apiVersion: v1
kind: Service
metadata:
  name: student-service
  namespace: student-management
  labels:
    app: student-service
spec:
  selector:
    app: student-service
  ports:
  - protocol: TCP
    port: 8003 # Cluster içi erişim portu
    targetPort: 8003 # Container portu
